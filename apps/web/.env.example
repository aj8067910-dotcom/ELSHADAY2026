# URL pública da API (NestJS no Render).
# Na Vercel, defina esta variável em Settings → Environment Variables.
# NÃO coloque DATABASE_URL aqui — o site não fala direto com o banco,
# ele fala com a API. O DATABASE_URL vai no serviço da API (Render).
API_URL="https://elshaday-api.onrender.com"
